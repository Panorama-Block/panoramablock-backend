# DCA System - Architecture Overview

## 🏗️ System Architecture

```
┌──────────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                                 │
│                                                                      │
│  ┌────────────────────┐         ┌────────────────────┐             │
│  │  /account page     │         │   /dca page        │             │
│  │                    │         │                    │             │
│  │ - Create Wallet    │────────▶│ - Create Strategy  │             │
│  │ - View Wallets     │         │ - View Strategies  │             │
│  │ - Deposit/Withdraw │         │ - View History     │             │
│  └────────────────────┘         └─────────┬──────────┘             │
│                                            │                         │
└────────────────────────────────────────────┼─────────────────────────┘
                                             │
                                             │ HTTP/REST
                                             ↓
┌──────────────────────────────────────────────────────────────────────┐
│                    DCA SERVICE (Port 3004)                           │
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                       Express Server                          │ │
│  │                                                               │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐        │ │
│  │  │ DCA Routes  │  │Transaction  │  │ Smart Account│        │ │
│  │  │             │  │  Routes     │  │   Routes     │        │ │
│  │  │ /dca/*      │  │/transaction/*│  │              │        │ │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬───────┘        │ │
│  │         │                │                │                 │ │
│  │         └────────────────┴────────────────┘                 │ │
│  │                          │                                  │ │
│  └──────────────────────────┼──────────────────────────────────┘ │
│                             │                                    │
│  ┌──────────────────────────┼──────────────────────────────────┐ │
│  │                    Services Layer                           │ │
│  │                          │                                  │ │
│  │  ┌────────────┐   ┌──────▼───────┐   ┌─────────────┐     │ │
│  │  │ DCA        │   │Transaction   │   │ Smart       │     │ │
│  │  │ Service    │◀──│  Service     │──▶│ Account     │     │ │
│  │  │            │   │              │   │  Service    │     │ │
│  │  └─────┬──────┘   └──────────────┘   └──────┬──────┘     │ │
│  │        │                                     │            │ │
│  └────────┼─────────────────────────────────────┼────────────┘ │
│           │                                     │              │
│  ┌────────▼─────────────────────────────────────▼────────────┐ │
│  │                  Background Jobs                          │ │
│  │                                                           │ │
│  │  ┌──────────────────────────────────────────────────┐   │ │
│  │  │          DCA Executor (Cron Job)                 │   │ │
│  │  │                                                  │   │ │
│  │  │  ⏰ Runs every minute                            │   │ │
│  │  │  1. Fetch ready strategies (nextExecution <= now)│  │ │
│  │  │  2. Get session key (encrypted)                 │   │ │
│  │  │  3. Connect to smart wallet                     │   │ │
│  │  │  4. Execute swap on Uniswap V3                  │   │ │
│  │  │  5. Record history                              │   │ │
│  │  │  6. Reschedule next execution                   │   │ │
│  │  └──────────────────────────────────────────────────┘   │ │
│  └──────────────────────────────────────────────────────────┘ │
│                             │                                  │
└─────────────────────────────┼──────────────────────────────────┘
                              │
                              ↓
┌──────────────────────────────────────────────────────────────────────┐
│                      REDIS DATABASE (Port 6380)                      │
│                                                                      │
│  ┌────────────────────┐  ┌────────────────────┐  ┌──────────────┐ │
│  │  Smart Accounts    │  │  DCA Strategies    │  │  Scheduled   │ │
│  │                    │  │                    │  │    Queue     │ │
│  │  Hash:             │  │  Hash:             │  │              │ │
│  │  smart-account:*   │  │  dca-strategy:*    │  │  Sorted Set: │ │
│  │                    │  │                    │  │  dca-scheduled│ │
│  │  - userId          │  │  - smartAccountId  │  │              │ │
│  │  - name            │  │  - fromToken       │  │  Score:      │ │
│  │  - sessionKey      │  │  - toToken         │  │  timestamp   │ │
│  │  - permissions     │  │  - amount          │  │              │ │
│  │  - expiresAt       │  │  - interval        │  │              │ │
│  └────────────────────┘  │  - nextExecution   │  └──────────────┘ │
│                          │  - isActive        │                   │
│  ┌────────────────────┐  └────────────────────┘  ┌──────────────┐ │
│  │  Session Keys      │                          │   History    │ │
│  │  (Encrypted)       │                          │              │ │
│  │                    │                          │  List:       │ │
│  │  String:           │                          │  dca-history:*│ │
│  │  session-key:*     │                          │              │ │
│  │                    │                          │  - timestamp │ │
│  │  AES-256 Encrypted │                          │  - txHash    │ │
│  └────────────────────┘                          │  - status    │ │
│                                                  └──────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌──────────────────────────────────────────────────────────────────────┐
│                      BLOCKCHAIN LAYER                                │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                    Thirdweb SDK v5                             │ │
│  │                                                                │ │
│  │  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐ │ │
│  │  │ Session Key  │────▶│ Smart Wallet │────▶│  Blockchain  │ │ │
│  │  │  (Backend)   │     │  (ERC-4337)  │     │  (Ethereum)  │ │ │
│  │  └──────────────┘     └──────────────┘     └──────────────┘ │ │
│  │                                                                │ │
│  │  Flow:                                                         │ │
│  │  1. Load session key from Redis (encrypted)                   │ │
│  │  2. Decrypt with ENCRYPTION_PASSWORD                          │ │
│  │  3. Create personal account from private key                  │ │
│  │  4. Connect to smart wallet using session key                 │ │
│  │  5. Prepare transaction (Uniswap swap)                        │ │
│  │  6. Sign with session key (within permissions)                │ │
│  │  7. Send as User Operation (bundled)                          │ │
│  │  8. Return transaction hash                                   │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                    Uniswap V3 Router                           │ │
│  │              0xE592427A0AEce92De3Edee1F18E0157C...             │ │
│  │                                                                │ │
│  │  Functions used:                                               │ │
│  │  - exactInputSingle() → Swap tokens                           │ │
│  │  - approve() → Approve ERC20 spending (if needed)             │ │
│  └────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow - Creating a DCA Strategy

```
┌─────────┐
│  USER   │
└────┬────┘
     │ 1. Clicks "Create Recurring Buy"
     ↓
┌─────────────────┐
│ Frontend Modal  │
│                 │
│ - Amount: 0.01  │
│ - From: ETH     │
│ - To: USDC      │
│ - Interval: daily│
└────┬────────────┘
     │ 2. POST /dca/create-strategy
     ↓
┌────────────────────────────────────────┐
│ DCA Service                            │
│                                        │
│ ┌────────────────────────────────────┐│
│ │ 1. Validate smart account exists   ││
│ │ 2. Calculate nextExecution          ││
│ │    now + 24 hours                   ││
│ │ 3. Generate strategyId              ││
│ │    {accountId}-{timestamp}          ││
│ └────────────────────────────────────┘│
└────┬───────────────────────────────────┘
     │ 3. Save to Redis
     ↓
┌──────────────────────────────────────────┐
│ Redis                                    │
│                                          │
│ HSET dca-strategy:{id}                   │
│   smartAccountId: 0x...                  │
│   fromToken: 0x0000...                   │
│   toToken: 0xa0b8...                     │
│   amount: "0.01"                         │
│   interval: "daily"                      │
│   nextExecution: 1234567890              │
│   isActive: "true"                       │
│                                          │
│ ZADD dca-scheduled                       │
│   1234567890 → {strategyId}              │
│                                          │
│ SADD account:strategies:{accountId}      │
│   {strategyId}                           │
└────┬─────────────────────────────────────┘
     │ 4. Return response
     ↓
┌─────────────────┐
│   Frontend      │
│                 │
│ ✅ Success!     │
│ Next: 24h       │
└─────────────────┘
```

---

## ⚙️ Execution Flow - Cron Job

```
⏰ Every minute...

┌──────────────────┐
│  DCA Executor    │
└────┬─────────────┘
     │ 1. Query Redis
     ↓
┌──────────────────────────────────┐
│ ZRANGEBYSCORE dca-scheduled      │
│   0 → currentTimestamp           │
│                                  │
│ Returns: [strategyId1, ...]      │
└────┬─────────────────────────────┘
     │ 2. For each ready strategy
     ↓
┌──────────────────────────────────────────────┐
│ A. Load strategy data                        │
│    HGETALL dca-strategy:{id}                 │
│                                              │
│ B. Check if active                           │
│    if isActive !== "true" → skip             │
│                                              │
│ C. Get session key (encrypted)               │
│    GET session-key:{smartAccountId}          │
│    → decrypt with ENCRYPTION_PASSWORD        │
│                                              │
│ D. Check expiration                          │
│    if expired → mark inactive, skip          │
└────┬─────────────────────────────────────────┘
     │ 3. Execute swap
     ↓
┌──────────────────────────────────────────────┐
│ executeSwap()                                │
│                                              │
│ 1. Initialize Thirdweb client                │
│ 2. Create personal account from session key  │
│ 3. Connect to smart wallet                   │
│ 4. Detect token type (ETH vs ERC20)          │
│                                              │
│ IF ETH:                                      │
│   - Prepare Uniswap exactInputSingle         │
│   - Set value = amount                       │
│   - Send transaction                         │
│                                              │
│ IF ERC20:                                    │
│   - Check allowance                          │
│   - Approve if needed                        │
│   - Prepare Uniswap exactInputSingle         │
│   - Send transaction                         │
│                                              │
│ 5. Wait for transaction confirmation         │
│ 6. Return txHash                             │
└────┬─────────────────────────────────────────┘
     │ 4. Record result
     ↓
┌──────────────────────────────────────────────┐
│ LPUSH dca-history:{smartAccountId}           │
│   {                                          │
│     timestamp: 1234567890,                   │
│     txHash: "0xabc...",                      │
│     amount: "0.01",                          │
│     fromToken: "0x...",                      │
│     toToken: "0x...",                        │
│     status: "success"                        │
│   }                                          │
└────┬─────────────────────────────────────────┘
     │ 5. Reschedule
     ↓
┌──────────────────────────────────────────────┐
│ updateStrategyAfterExecution()               │
│                                              │
│ 1. Calculate next execution                  │
│    now + interval (24h for daily)            │
│                                              │
│ 2. Update strategy                           │
│    HSET dca-strategy:{id}                    │
│      lastExecuted: now                       │
│      nextExecution: now+24h                  │
│                                              │
│ 3. Update scheduled queue                    │
│    ZADD dca-scheduled                        │
│      (now+24h) → {strategyId}                │
└──────────────────────────────────────────────┘

✅ Done! Wait for next cron cycle...
```

---

## 🔐 Security Model

```
┌─────────────────────────────────────────────────────────────┐
│                    Session Key Lifecycle                    │
└─────────────────────────────────────────────────────────────┘

1. CREATION (Backend Only)
   ┌──────────────────────────────────────┐
   │ SmartAccountService                  │
   │                                      │
   │ 1. Generate private key (random)     │
   │ 2. Encrypt with AES-256             │
   │    - Password: ENCRYPTION_PASSWORD   │
   │    - Algorithm: aes-256-cbc          │
   │ 3. Store encrypted in Redis          │
   │ 4. NEVER send to frontend            │
   └──────────────────────────────────────┘

2. STORAGE
   ┌──────────────────────────────────────┐
   │ Redis                                │
   │                                      │
   │ session-key:{smartAccountAddress}    │
   │   → encrypted_private_key            │
   │                                      │
   │ ⚠️ Value is encrypted blob           │
   │ ⚠️ Cannot be decrypted without env   │
   └──────────────────────────────────────┘

3. USAGE (Backend Only)
   ┌──────────────────────────────────────┐
   │ TransactionService                   │
   │                                      │
   │ 1. Retrieve encrypted key from Redis │
   │ 2. Decrypt with ENCRYPTION_PASSWORD  │
   │ 3. Use in-memory (never persist)     │
   │ 4. Sign transaction                  │
   │ 5. Clear from memory                 │
   └──────────────────────────────────────┘

4. PERMISSIONS
   ┌──────────────────────────────────────┐
   │ Session Key Constraints              │
   │                                      │
   │ ✅ approvedTargets: ["*"]            │
   │    Can call any contract             │
   │                                      │
   │ ✅ nativeTokenLimit: "0.1"           │
   │    Max 0.1 ETH per transaction       │
   │                                      │
   │ ✅ Time window: 30 days              │
   │    Auto-expires after duration       │
   └──────────────────────────────────────┘

5. VALIDATION (Every Transaction)
   ┌──────────────────────────────────────┐
   │ TransactionService.validateTransaction│
   │                                      │
   │ ✅ Check owner (userId matches)      │
   │ ✅ Check expiration (still valid)    │
   │ ✅ Check target (in approved list)   │
   │ ✅ Check amount (within limit)       │
   │ ✅ Check time window                 │
   │                                      │
   │ ❌ Any check fails → reject          │
   └──────────────────────────────────────┘
```

---

## 🌐 Network Flow

```
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │ HTTPS
       ↓
┌──────────────────────────────┐
│  Next.js Frontend            │
│  localhost:7778/miniapp      │
│                              │
│  - /dca                      │
│  - /account                  │
└──────┬───────────────────────┘
       │ HTTP (localhost)
       ↓
┌──────────────────────────────┐
│  DCA Service                 │
│  localhost:3004              │
│                              │
│  REST API endpoints          │
└──────┬───────────────────────┘
       │ TCP
       ↓
┌──────────────────────────────┐
│  Redis                       │
│  localhost:6380              │
│                              │
│  Database                    │
└──────────────────────────────┘

       ↓ (Blockchain calls)

┌──────────────────────────────┐
│  Thirdweb Infrastructure     │
│                              │
│  - RPC endpoints             │
│  - Bundler (ERC-4337)        │
└──────┬───────────────────────┘
       │ HTTPS
       ↓
┌──────────────────────────────┐
│  Ethereum Mainnet            │
│                              │
│  - Smart Wallets             │
│  - Uniswap V3                │
└──────────────────────────────┘
```

---

**Last Updated**: 2025-11-11
**Version**: 1.0.0
